# DRVT Mall

쇼핑몰 풀스택 프로젝트

## 구조

- `Client/` — React + Vite 프론트엔드
- `Server/` — Node.js + Express + MongoDB API

## 시작하기

### Server

```bash
cd Server
npm install
cp .env.example .env
npm run dev
```

### Client

```bash
cd Client
npm install
cp .env.example .env
npm run dev
```
